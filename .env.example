export HF_TOKEN=hf_your_token_here

# MongoDB observability (Phase 5)
# Default connects to a local MongoDB instance — no auth required for local dev.
export MONGODB_URI=mongodb://localhost:27017
export MONGODB_DB=med360
